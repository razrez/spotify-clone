@{
    Layout = "MainLayout";
    ViewData["Title"] = "Artist";
}

<link rel="stylesheet" href="~/css/play-button.css" asp-append-version="true"/>
<div class="content-spacing">
    <div class="artist-header">
        <div>&nbsp;</div>
        <div class="artist-info">
            <div class="verified-icon"><span>Verified Artist</span></div>
            <div class="artist-name">John Frusciante</div>
        </div>
    </div> 
    <div class="artist-body">
        <div class="artist-interaction">
            <div class="play-button active"></div> 
            <div class="follow-button">FOLLOW</div>
        </div>
        <div class="artist-releases">
            <div class="content-header">
                <a href="#" class="content-type">Albums</a>
                <a href="#" class="see-discography">SEE DISCOGRAPHY</a>
            </div>
            <div class="content-body" id="albums">
            </div>
            <div class="content-header">
                <a href="#" class="content-type">Singles and EPs</a>
                <a href="#" class="see-discography">SEE DISCOGRAPHY</a>
            </div>
            <div class="content-body" id="singles-and-eps"></div>
        </div>
    </div>
</div>
<script src="~/js/play-button.js"></script>
@* CARDS MOCKS and EXAMPLES of USAGE also in src*@
<script src="~/js/cards/playlist-card.js"></script>
<script src="~/js/cards/profile-card.js"></script>
<script>
        document.querySelector("#albums")
            .appendChild(new PlaylistCard("TESTS-ARTIST-ALBUM.jpg", "Title", "Album", 1)
            .render());
        document.querySelector("#albums")
            .appendChild(new PlaylistCard("TESTS-ARTIST-ALBUM.jpg", "Title11111111111111",  "Album", 2)
            .render());
        document.querySelector("#singles-and-eps")
            .appendChild(new PlaylistCard("TESTS-ARTIST-ALBUM.jpg", "Title", "Single", 3)
            .render());
        document.querySelector("#singles-and-eps")
            .appendChild(new PlaylistCard("TESTS-ARTIST-ALBUM.jpg", "Title", "EP", 4)
            .render());
        document.querySelector("#singles-and-eps")
            .appendChild(new PlaylistCard("TESTS-ARTIST-ALBUM.jpg", "Title", "PLAYLIST", 5, "USER")
            .render());
        document.querySelector("#singles-and-eps")
            .appendChild(new PlaylistCard("", "Title", "PLAYLIST", 6, "punkRelique")
            .render());
        document.querySelector("#singles-and-eps")
            .appendChild(new ProfileCard("", "punkRelique", "Profile", 1)
            .render());
        document.querySelector("#singles-and-eps")
            .appendChild(new ProfileCard("", "BBBBBBBBBBBBBB", "Artist", 2)
            .render()); 
</script>